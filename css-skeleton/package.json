{
  "name": "css-skeleton",
  "version": "1.0.0",
  "main": "scripts.js",
  "scripts": {
    "dev": "browser-sync start --server --files '*.html, *.css, *.js' --port 8080 --no-notify",
    "serve": "http-server -p 8080 -o",
    "format": "prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "http-server": "^14.1.1",
    "prettier": "^3.2.5"
  }
}
